export function RankList({ title, items }: { title: string; items: { label: string; value: number }[] }) {
  const max = Math.max(...items.map((item) => item.value), 1);
  return <div className="rounded-lg border border-slate-800 bg-slate-900/45 p-4"><h3 className="mb-4 text-sm font-medium text-slate-200">{title}</h3><div className="space-y-3">{items.length ? items.slice(0, 8).map((item, index) => <div key={item.label}><div className="mb-1 flex items-center justify-between gap-4 font-mono text-xs"><span className="truncate text-slate-400"><span className="mr-2 text-slate-700">{String(index + 1).padStart(2, "0")}</span>{item.label}</span><span className="text-slate-500">{item.value}</span></div><div className="h-1 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-cyan-300/65" style={{ width: `${(item.value / max) * 100}%` }} /></div></div>) : <p className="py-8 text-center text-xs text-slate-600">Нет данных за период</p>}</div></div>;
}
